<Window x:Class="MultiOtpManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="multiOTP Manager"
        Width="1100" Height="720" MinWidth="900" MinHeight="580"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        Closed="Window_Closed" Loaded="Window_Loaded">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#F3F5F7" />
        <SolidColorBrush x:Key="PanelBackground" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PanelBorder" Color="#D5DBE1" />
        <SolidColorBrush x:Key="TextColor" Color="#223038" />
        <SolidColorBrush x:Key="MutedTextColor" Color="#61707B" />
        <SolidColorBrush x:Key="AccentColor" Color="#236C72" />
        <SolidColorBrush x:Key="AccentMouseOverColor" Color="#2C858C" />
        <SolidColorBrush x:Key="SecondaryBackgroundColor" Color="#E7EBEF" />
        <SolidColorBrush x:Key="SecondaryMouseOverColor" Color="#D8DEE4" />
        <SolidColorBrush x:Key="GoodColor" Color="#1D6D38" />
        <SolidColorBrush x:Key="BadColor" Color="#A33131" />
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="88" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource SecondaryBackgroundColor}" />
            <Setter Property="BorderBrush" Value="#B8C0C8" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="{StaticResource SecondaryMouseOverColor}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="B" Property="Background" Value="#EDF0F2" />
                                <Setter TargetName="B" Property="BorderBrush" Value="#D5DBE1" />
                                <Setter Property="Foreground" Value="#98A3AB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentColor}" />
            <Setter Property="BorderBrush" Value="#1B575C" />
            <Setter Property="Foreground" Value="White" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="BorderBrush" Value="#B8C0C8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="BorderBrush" Value="#B8C0C8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="7,3" />
            <Setter Property="BorderBrush" Value="#B8C0C8" />
            <Setter Property="Background" Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="Panel" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="14" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
        <Style x:Key="FieldCaption" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource MutedTextColor}" />
            <Setter Property="Margin" Value="0,0,0,3" />
        </Style>
        <Style x:Key="FieldValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextColor}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
    </Window.Resources>
    <DockPanel Background="{StaticResource WindowBackground}">
        <Border DockPanel.Dock="Top" Background="#FFFFFF" BorderBrush="#D5DBE1" BorderThickness="0,0,0,1" Padding="20,10,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="28" Height="28" Background="{StaticResource AccentColor}" CornerRadius="3">
                        <TextBlock Text="mO" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="multiOTP Manager" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextColor}" Margin="10,0,0,0" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Grid.Column="1" x:Name="ExecutablePathText" Text="multiotp.exe" Foreground="{StaticResource MutedTextColor}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="20,0,20,0" ToolTip="multiotp.exe location" />
                <Button Grid.Column="2" x:Name="CancelButton" Content="Cancel" Width="82" Click="CancelButton_Click" IsEnabled="False" />
            </Grid>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="#FFFFFF" BorderBrush="#D5DBE1" BorderThickness="0,1,0,0" Padding="20,7,20,7">
            <TextBlock x:Name="StatusText" Text="Ready" Foreground="{StaticResource MutedTextColor}" TextTrimming="CharacterEllipsis" />
        </Border>
        <TabControl Margin="12" BorderThickness="0" Background="Transparent">
            <!-- Authentication -->
            <TabItem Header="Authentication">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Authentication test" Style="{StaticResource SectionTitle}" />
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="USERNAME" Style="{StaticResource FieldCaption}" />
                                <TextBox x:Name="UsernameBox" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="OTP" Style="{StaticResource FieldCaption}" />
                                <PasswordBox x:Name="OtpBox" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="TIMEOUT (SECONDS)" Style="{StaticResource FieldCaption}" />
                                <TextBox x:Name="TimeoutBox" Text="5" />
                            </StackPanel>
                            <CheckBox x:Name="UseVerifySwitchBox" Content="Use -verify command" Margin="0,0,0,14" Foreground="{StaticResource TextColor}" />
                            <Button x:Name="VerifyButton" Style="{StaticResource PrimaryButton}" Content="Verify" Width="104" Click="VerifyButton_Click" HorizontalAlignment="Left" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource Panel}" Margin="12,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Result" Style="{StaticResource SectionTitle}" />
                            <Border Grid.Row="1" Background="#F7F9FA" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" CornerRadius="3" Padding="12,9" Margin="0,0,0,10">
                                <TextBlock x:Name="VerifyResultText" Text="No test run" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource MutedTextColor}" TextWrapping="Wrap" />
                            </Border>
                            <TextBox Grid.Row="2" x:Name="VerifyOutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Users -->
            <TabItem Header="Users">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Users" Style="{StaticResource SectionTitle}" />
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                <Button x:Name="RefreshUsersButton" Style="{StaticResource PrimaryButton}" Content="Refresh" Click="RefreshUsersButton_Click" Margin="0,0,6,0" />
                                <Button x:Name="CreateUserButton" Content="Create" Click="CreateUserButton_Click" Margin="0,0,6,0" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="NEW USER" Style="{StaticResource FieldCaption}" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBox x:Name="NewUsernameBox" Width="150" />
                            </StackPanel>
                            <ListBox Grid.Row="3" x:Name="UsersListBox" SelectionChanged="UsersListBox_SelectionChanged" BorderBrush="{StaticResource PanelBorder}" Background="White" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,7">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextColor}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Foreground="{StaticResource MutedTextColor}" FontSize="11" TextTrimming="CharacterEllipsis">
                                                <Run Text="{Binding TokenType}" />
                                                <Run Text="  " />
                                                <Run Text="{Binding Status}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource Panel}" Margin="12,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,10">
                                    <TextBlock Text="USERNAME" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailUsernameText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,12,10">
                                    <TextBlock Text="TOKEN TYPE" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailTokenTypeText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,10">
                                    <TextBlock Text="STATUS" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailStatusText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,12,0">
                                    <TextBlock Text="CREATED" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailCreatedText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,0,12,0">
                                    <TextBlock Text="OTP DIGITS" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailDigitsText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2" Grid.Row="1">
                                    <TextBlock Text="DESCRIPTION" Style="{StaticResource FieldCaption}" />
                                    <TextBlock x:Name="DetailDescriptionText" Text="-" Style="{StaticResource FieldValue}" />
                                </StackPanel>
                            </Grid>
                            <TextBlock Grid.Row="1" Text="Details" Style="{StaticResource SectionTitle}" />
                            <TextBox Grid.Row="2" x:Name="UserDetailBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />
                            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                                <Button x:Name="ActivateBtn" Content="Activate" Click="ActivateBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="DeactivateBtn" Content="Deactivate" Click="DeactivateBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="LockBtn" Content="Lock" Click="LockBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="UnlockBtn" Content="Unlock" Click="UnlockBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="ResyncBtn" Content="Resync" Click="ResyncBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="QrCodeBtn" Content="QR Code" Click="QrCodeBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="DisablePinBtn" Content="Disable PIN" Click="DisablePinBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="DeleteUserBtn" Content="Delete" Click="DeleteUserBtn_Click" Margin="0,0,6,0" Foreground="#A33131" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Tokens -->
            <TabItem Header="Tokens">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Token list" Style="{StaticResource SectionTitle}" />
                            <Button Grid.Row="1" x:Name="RefreshTokensButton" Style="{StaticResource PrimaryButton}" Content="Refresh" Click="RefreshTokensButton_Click" HorizontalAlignment="Left" Margin="0,0,0,10" />
                            <TextBox Grid.Row="2" x:Name="TokensOutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />
                        </Grid>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource Panel}" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Token operations" Style="{StaticResource SectionTitle}" />
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="USERNAME" Style="{StaticResource FieldCaption}" />
                                <TextBox x:Name="TokenUsernameBox" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="TOKEN ID" Style="{StaticResource FieldCaption}" />
                                <TextBox x:Name="TokenIdBox" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="AssignTokenBtn" Content="Assign" Click="AssignTokenBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="RemoveTokenBtn" Content="Remove" Click="RemoveTokenBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="DeleteTokenBtn" Content="Delete" Click="DeleteTokenBtn_Click" Margin="0,0,6,0" Foreground="#A33131" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Logs -->
            <TabItem Header="Logs">
                <Grid Margin="0,10,0,0">
                    <Border Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Log viewer" Style="{StaticResource SectionTitle}" />
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                <Button x:Name="ShowLogBtn" Style="{StaticResource PrimaryButton}" Content="Show Log" Click="ShowLogBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="ClearLogBtn" Content="Clear Log" Click="ClearLogBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="ErrorCodesBtn" Content="Error Codes" Click="ErrorCodesBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="VersionBtn" Content="Version" Click="VersionBtn_Click" Margin="0,0,6,0" />
                            </StackPanel>
                            <TextBox Grid.Row="2" x:Name="LogOutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <!-- AD/LDAP -->
            <TabItem Header="AD/LDAP">
                <Grid Margin="0,10,0,0">
                    <Border Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="AD/LDAP integration" Style="{StaticResource SectionTitle}" />
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                <Button x:Name="LdapCheckBtn" Style="{StaticResource PrimaryButton}" Content="Check Connection" Click="LdapCheckBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="LdapUsersListBtn" Content="List LDAP Users" Click="LdapUsersListBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="LdapSyncBtn" Content="Sync Users" Click="LdapSyncBtn_Click" Margin="0,0,6,0" />
                            </StackPanel>
                            <TextBox Grid.Row="2" x:Name="LdapOutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Credential Provider -->
            <TabItem Header="Credential Provider">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Style="{StaticResource Panel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,14,0">
                                <TextBlock Text="LOGON SCOPE" Style="{StaticResource FieldCaption}" />
                                <ComboBox x:Name="LogonScopeBox" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="Local and remote" Tag="0" />
                                    <ComboBoxItem Content="Remote desktop only" Tag="1" />
                                    <ComboBoxItem Content="Local only" Tag="2" />
                                    <ComboBoxItem Content="Disabled" Tag="3" />
                                </ComboBox>
                                <TextBlock Text="LOGON PROVIDERS" Style="{StaticResource FieldCaption}" Margin="0,10,0,0" />
                                <ComboBox x:Name="LogonExclusiveBox" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="multiOTP only" Tag="e" />
                                    <ComboBoxItem Content="Other providers allowed" Tag="d" />
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="0,0,14,0">
                                <TextBlock Text="UNLOCK SCOPE" Style="{StaticResource FieldCaption}" />
                                <ComboBox x:Name="UnlockScopeBox" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="Local and remote" Tag="0" />
                                    <ComboBoxItem Content="Remote desktop only" Tag="1" />
                                    <ComboBoxItem Content="Local only" Tag="2" />
                                    <ComboBoxItem Content="Disabled" Tag="3" />
                                </ComboBox>
                                <TextBlock Text="UNLOCK PROVIDERS" Style="{StaticResource FieldCaption}" Margin="0,10,0,0" />
                                <ComboBox x:Name="UnlockExclusiveBox" SelectedValuePath="Tag">
                                    <ComboBoxItem Content="multiOTP only" Tag="e" />
                                    <ComboBoxItem Content="Other providers allowed" Tag="d" />
                                </ComboBox>
                            </StackPanel>
                            <Button Grid.Column="2" x:Name="SaveSettingsButton" Style="{StaticResource PrimaryButton}" Content="Save" VerticalAlignment="Bottom" Click="SaveSettingsButton_Click" />
                        </Grid>
                    </Border>
                    <Border Grid.Row="1" Style="{StaticResource Panel}" Margin="0,12,0,0">
                        <StackPanel>
                            <TextBlock Text="Authentication flow" Style="{StaticResource SectionTitle}" />
                            <CheckBox x:Name="TwoStepHideOtpBox" Content="Ask for OTP in a second step" FontSize="13" Foreground="{StaticResource TextColor}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <!-- Maintenance -->
            <TabItem Header="Maintenance">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource Panel}">
                        <StackPanel>
                            <TextBlock Text="Backup &amp; Restore" Style="{StaticResource SectionTitle}" />
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="PASSWORD" Style="{StaticResource FieldCaption}" />
                                <PasswordBox x:Name="BackupPasswordBox" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="BackupBtn" Content="Backup Config" Click="BackupBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="RestoreBtn" Content="Restore Config" Click="RestoreBtn_Click" Margin="0,0,6,0" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource Panel}" Margin="12,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Cleanup" Style="{StaticResource SectionTitle}" />
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="PurgeLockBtn" Content="Purge Lock Files" Click="PurgeLockBtn_Click" Margin="0,0,6,0" />
                                <Button x:Name="PurgeLdapCacheBtn" Content="Purge LDAP Cache" Click="PurgeLdapCacheBtn_Click" Margin="0,0,6,0" />
                            </StackPanel>
                            <TextBlock Text="Output" Style="{StaticResource SectionTitle}" Margin="0,16,0,8" />
                            <TextBox x:Name="MaintenanceOutputBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" Height="200" />
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
